# Tools that BUILD the JupyterLite site (locally and in GitHub Actions).
# Nothing here is installed in the browser: the notebooks install ModelFlow there with %pip.
jupyterlite-core==0.8.3
jupyterlab~=4.6.3
notebook~=7.6.2

# Python kernel running in the browser (Pyodide, Python 3.14)
jupyterlite-pyodide-kernel==0.8.6

# Front-end parts of the widget libraries ModelFlow imports; installing them here
# puts their JupyterLab extensions into the site
ipywidgets>=8.1.3,<9
ipydatagrid
